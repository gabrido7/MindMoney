PORT=3001

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=mindmoney
DB_USER=mindmoney_app
DB_PASSWORD=troque_esta_senha_em_producao

JWT_SECRET=troque_este_segredo_por_algo_aleatorio_e_longo
JWT_EXPIRES_IN=7d

CORS_ORIGIN=http://localhost:5173

# Recurso inteligente (etapa 15): hoje 100% baseado em regras determinísticas
# sobre dados reais (backend/src/modules/insights). Nenhuma integração de IA
# externa está configurada ou implementada — não invente uma chave aqui.
# Se um provedor real for adicionado no futuro, a variável de configuração
# entra abaixo (nome exato depende do provedor escolhido), e o código deve
# checar sua presença antes de tentar usá-la, mantendo o caminho
# determinístico como fallback.
# AI_PROVIDER_API_KEY=
